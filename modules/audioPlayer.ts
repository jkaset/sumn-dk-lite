const AudioPlayer = () => {

    //when clicked, title and play button start autoplay of audio 
    //and title and track populate m-playBar
   
}

export default AudioPlayer


// $('[data-song-url]').parents('a').on('click', function (event) {
//     var target = event.currentTarget
//     var data = $(target).find('span').data()
//     // {songName: "Sleek", songUrl: "https://...."}
//     $('[player="song-title"]').text(data.songName)
//     //$('[player=""]').data({playerURL: data.songURL})
//   })